import React, { PureComponent, Fragment } from 'react'
import Button from '../../components/Button'

class App extends PureComponent {
  render () {
    return (
      <Fragment>
        <Button primary>React 16.2 with Parcel bundler</Button>
      </Fragment>
    )
  }
}

export default App
